// Entry point for the FASDELY guest Mini App.
// Screens and app logic land in later tasks of this plan; this placeholder
// exists only so `index.html`'s <script type="module" src="/src/main.ts">
// resolves and `vite build` succeeds for this scaffolding task.
export {};
